# Data

Raw data should not be committed to GitHub.

The assignment dataset is:

`harshitshankhdhar/imdb-dataset-of-top-1000-movies-and-tv-shows`

The final notebook should download the dataset during execution using the Kaggle
API. Keep real Kaggle credentials outside the repository. Before submission,
replace any visible credential values with placeholders.

Expected raw file after download and extraction:

`imdb_top_1000.csv`

